CREATE TABLE detalle_venta (
  id_tienda BigInt(5) NOT NULL,
  num_orden VarChar(8) NOT NULL,
  id_titulo VarChar(10) NOT NULL,
  cantidad  BigInt(100),
  descuento BigInt(4), 
  PRIMARY KEY (
      id_tienda
  )
) ;
ALTER TABLE detalle_venta COMMENT = '';
